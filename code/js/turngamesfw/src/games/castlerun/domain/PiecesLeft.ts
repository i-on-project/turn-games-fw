export class PiecesLeft {
    alpha: number;
    beta: number;
    forAlpha: number;
    forBeta: number;

    constructor(alpha: number, beta: number, forAlpha: number, forBeta: number) {
        this.alpha = alpha;
        this.beta = beta;
        this.forAlpha = forAlpha;
        this.forBeta = forBeta;
    }
}
